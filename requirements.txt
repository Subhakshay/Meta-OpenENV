fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
openai>=1.30.0
aiosqlite>=0.19.0      # always needed (SQLite local dev)
asyncpg>=0.29.0        # only needed if DATABASE_URL is postgres
requests
python-dotenv>=1.0.0
